spec:
  template:
    spec:
      containers:
      - name: metrics-server
        command:
        - /metrics-server
        - --v=2
        - --kubelet-insecure-tls
        - --kubelet-preferred-address-types=InternalIP
